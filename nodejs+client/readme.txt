Backend - nodejs server, port 3001
Frontend - in the folder "book-client", created using "react-create-app" (webpack local development server), port 3000

Connection via proxy (package.json on the frontend)

Launch via Docker: $docker-compose up